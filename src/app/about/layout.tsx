export const metadata = {
  title: "About | RealtorSite",
  description: "Learn about the realtor and real estate services.",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

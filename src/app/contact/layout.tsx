export const metadata = {
  title: "Contact | RealtorSite",
  description: "Contact the realtor for buying, selling, or property inquiries.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

export const metadata = {
  title: "Listings | RealtorSite",
  description: "Browse active real estate listings in British Columbia.",
};

export default function ListingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
